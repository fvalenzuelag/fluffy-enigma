# Copia este archivo a .env en la misma carpeta y ajusta los valores.
# El archivo .env no debe subirse al repositorio (está en .gitignore).

MYSQL_ROOT_PASSWORD=cambiar-root
MYSQL_DATABASE=dockerlab
MYSQL_USER=dockerlab
MYSQL_PASSWORD=cambiar-app
